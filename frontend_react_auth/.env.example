# Frontend environment variables
# PUBLIC_INTERFACE
# REACT_APP_API_BASE_URL is used by src/services/api.js to form API requests.
# Example local setting that targets the backend on port 3001:
REACT_APP_API_BASE_URL=http://localhost:3001
